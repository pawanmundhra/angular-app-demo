import { ReferenceDataFilter } from './ReferenceDataFilter';

describe('ReferenceDataFilter', () => {
  it('create an instance', () => {
    const pipe = new ReferenceDataFilter();
    expect(pipe).toBeTruthy();
  });
});
